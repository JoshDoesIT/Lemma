name: Security Scans

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  schedule:
    - cron: '0 0 * * 1' # Run weekly on Mondays

jobs:
  snyk-scan:
    name: Snyk SCA & Code Scan
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      
      # TODO: Phase 1
      # - name: Setup Node
      #   uses: actions/setup-node@v4
      #   with:
      #     node-version: 20
      
      # - name: Install dependencies
      #   run: npm ci
      
      # - name: Run Snyk SCA (Open Source)
      #   uses: snyk/actions/node@master
      #   env:
      #     SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
      #   with:
      #     command: test
      
      # - name: Run Snyk Code (SAST)
      #   uses: snyk/actions/node@master
      #   env:
      #     SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
      #   with:
      #     command: code test

  # TODO: Phase 1
  # license-check:
  #   name: License Compliance Check
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v6
  #     - name: Run license-checker
  #       run: npx license-checker --onlyAllow "Apache-2.0; MIT; ISC; BSD-2-Clause; BSD-3-Clause"

  sboms:
    name: Generate SBOM
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      # TODO: Phase 1
      # - name: Generate CycloneDX SBOM
      #   run: echo "Generate SBOM here"
