"""Reference connectors that ship with the SDK.

Used as the subject for ``lemma connector test`` smoke runs and as a
starting point for connector authors. Every reference connector
implements the same ``Connector`` contract third-party connectors use.
"""
