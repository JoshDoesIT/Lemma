"""First-party Lemma connectors.

Each connector under this namespace is a fully-maintained integration
against a real upstream service. Tests mock the transport layer so no
network calls happen in CI.
"""
